PROJECT(DemoProject)

FILE(GLOB_RECURSE PRJT_SOURCE
    "${CMAKE_CURRENT_SOURCE_DIR}/Source/*.cpp"
    "${CMAKE_CURRENT_SOURCE_DIR}/Source/*.h"
    "${CMAKE_CURRENT_SOURCE_DIR}/Source/*.hpp"
)

ADD_EXECUTABLE(DemoProject
    "${PRJT_SOURCE}"
)

SET(B33_TARGET DemoProject PARENT_SCOPE)
